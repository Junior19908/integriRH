// Apenas exemplos de interação leve/visual
(function () {
  const exportBtn = document.getElementById('btn-export');
  if (exportBtn) {
    exportBtn.addEventListener('click', () => {
      // Placeholder de exportação visual
      alert('Exportação CSV (visual) — integraremos de verdade depois.');
    });
  }
})();
